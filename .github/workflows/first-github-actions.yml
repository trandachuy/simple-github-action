name: Run Shell Command
on: [push]

jobs:
    run-shell-command:
        runs-on: ubuntu-latest
        steps:
            - name: Echo a string
              run: echo "Hello Security Groups, First Github actions"